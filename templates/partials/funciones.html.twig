{#    funciones predefinidas twig: algunas de las mas usuadas    #}
<h1>funciones</h1>
<h2>{{ nombre }}</h2>
{{ min ([9,6,8,1,2,0]) }} </br>
{{ max ([9,6,8,1,2,0]) }} </br>
{{ random(animales) }}
{% for i in range(0,100, 20) %}
    {{ i }} <br>
{% endfor %}

<h1>filtres</h1>
{{ animales|length }}</br>
{% set email = '         email@email.com       ' %}
{{ email|trim }}
{{ email|upper }}
{{ email|lower }}

<h1>Mis propias extensiones</h1>

{{ multiplicar(2)|raw }}   {#se agrefa el filtro nativo |raw para que se puede interpretar el html #}
{{ 12|multiplicar|raw }}